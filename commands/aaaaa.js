module.exports = function(msg,args){
    if(number = NaN) return;
    var i;
    number = args;
    if(number>50) return; // Failsafe because abuse
    for( i=0; i< number;i++){
        msg.channel.send(`aaaaaaaaaaaaaaaaaaaaaaaaaa`);
    }
};
